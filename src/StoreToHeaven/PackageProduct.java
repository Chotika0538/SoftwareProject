package StoreToHeaven;

public class PackageProduct extends Product {
    public PackageProduct(String name,String pattern,String detail,String path){
        super(name, pattern, detail, path);
    }
}
